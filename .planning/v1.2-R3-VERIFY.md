---
milestone: v1.2
round: R3
kind: verify
status: pending-operator
date: 2026-06-28
depends_on: R3 Plan A merged (STT engine seam) + Plan B/C merged (advise + docs) + R3 gate-enablement artifacts
gpu: REQUIRED for sign-off — sandbox steps are runnable locally; G1–G5 final numbers require the target GPU host
---

# R3 — Hardware-Aware STT Engines — Operator Verification

R3 is verified when Gates G1–G5 are signed on the target GPU host and the measured
numbers below are recorded here.

## Prereqs

- On `master` with `.env` present (`cp .env.example .env`).
- Dev harness installed: `python3 -m pip install -r requirements-dev.txt`.
- Both STT images bake the Parakeet bundle via `stt/fetch_parakeet_onnx.sh`.
- `sherpa-onnx~=1.13` and `jiwer~=4.0` are pinned in `requirements-dev.txt` / STT requirements.
- Confirm agent + both STT services receive `STT_ENGINE`, `STT_BUFFERED_DEVICE`,
  `STT_PARAKEET_MODEL`, and `VRAM_TOTAL_MB`.

---

## Gate G1 — Buffered Parakeet: real transcribe + WER + finalize latency  [ ]

- [sandbox] `python3 stt/test_engine.py` — real-body wiring with stubbed `sherpa_onnx`.
- [sandbox] `python3 scripts/stt-wer.py --self-check` — pure-logic OK; real-clip OK if
  `STT_PARAKEET_MODEL` points at fetched weights, otherwise GPU-deferred.
- [gpu] `docker compose build nemo-stt-cpu`.
- [gpu] `STT_ENGINE=buffered docker compose up -d nemo-stt-cpu`.
- [gpu] Parakeet WER: `python3 scripts/stt-wer.py`.
- [gpu] Streaming comparison: run the same fixture WAVs through streaming Nemotron and record WER.
- [gpu] Cut-off clip: `python3 scripts/stt-wer.py --cut-off rec.wav "<words>"`; expect
  `trailing_drop=False`.
- [gpu] Long-turn finalize latency: `docker compose logs agent | grep rolling_summary`; read
  `stt_ms.p50`.

**Record:** CPU-ONNX finalize p50 = ____ s · GPU-ONNX finalize p50 = ____ s ·
cut-off-free = Y/N · int8 WER (Parakeet) ____ / streaming ____

## Gate G2 — Hybrid co-residency VRAM → tune `VRAM_HYBRID_MB`  [ ]

- [sandbox] `bash scripts/vram-validate.sh --self-test`.
- [gpu] Streaming baseline:
  `STT_ENGINE=streaming VRAM_LIMIT_MB=24576 scripts/vram-validate.sh --stt-runtime gpu --with-kb`
  → peak `P_s`.
- [gpu] CUDA provider preflight:
  `STT_ENGINE=buffered STT_BUFFERED_DEVICE=gpu docker compose --profile stt-gpu up -d nemo-stt`;
  if sherpa-onnx CUDA provider fails, record it here and use `STT_BUFFERED_DEVICE=cpu` until a
  CUDA-capable sherpa build is pinned.
- [gpu] Hybrid:
  `STT_ENGINE=hybrid STT_BUFFERED_DEVICE=gpu VRAM_LIMIT_MB=24576 scripts/vram-validate.sh --stt-runtime gpu --with-kb`
  → peak `P_h`.
- [gpu] Derive threshold:
  `scripts/vram-validate.sh --derive-hybrid <P_h> <P_s>`.
- [gpu] Set `scripts/gpu-doctor.sh` `VRAM_HYBRID_MB` to `recommended_VRAM_HYBRID_MB`
  if the recommendation changes.

**Record:** Parakeet GPU MB = ____ · 16 GB hybrid peak = ____ MB ·
`VRAM_HYBRID_MB` set = ____

## Gate G3 — Hybrid EOU→final latency (`dur_ms`)  [ ]

- [gpu] Run `STT_ENGINE=hybrid STT_BUFFERED_DEVICE=gpu`.
- [gpu] Talk several turns; `docker compose logs agent | grep rolling_summary`.
- [gpu] `stt_ms.p50` is the EOU→final span; Nemotron deltas should still feel live.

**Record:** hybrid EOU→final p50 = ____ ms · partials felt live = Y/N

## Gate G4 — `streaming` byte-identical to pre-R3  [ ]

- [sandbox] `python3 stt/test_dispatch.py`.
- [sandbox] `python3 stt/test_engine.py`.
- [gpu] Run default `STT_ENGINE=streaming`; compare WS frames, transcripts, and P50 on the
  same audio as pre-R3.

**Record:** streaming P50 = ____ s (pre-R3 ____ s) · frames identical = Y/N

## Gate G5 — `finalize()` tail-drop: promote or document mitigated  [ ]

Candidates stay default-OFF in `stt/backend_common.py`:
`STT_THREAD_PRED_OUT=0`, `STT_FINALIZE_PAD=0`, `STT_FINALIZE_PAD_MS=560`.

- [gpu] Candidate A:
  `STT_THREAD_PRED_OUT=1 docker compose --profile stt-gpu up`, then
  `python3 scripts/stt-wer.py --cut-off rec.wav "<words>"`.
- [gpu] Candidate B:
  `STT_FINALIZE_PAD=1 docker compose --profile stt-gpu up`, then
  `python3 scripts/stt-wer.py --cut-off rec.wav "<words>"`.
- [gpu] If a candidate removes the drop with no WER regression, promote it by flipping its
  default ON. Otherwise document streaming tail-drop as mitigated by buffered/hybrid defaults.

**Record:** candidate A = pass/regress · candidate B = pass/regress ·
decision = promote ____ / mitigated

---

## R3 sign-off

All of G1–G5 signed on the target GPU host → **R3 VERIFIED**. Pin G1/G2 measured
numbers into this doc; if G5 promotes a candidate, flip its default in
`stt/backend_common.py` in the same change.
