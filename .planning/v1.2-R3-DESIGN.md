---
milestone: v1.2
stream: R3
kind: design
status: draft (A merged; B/C refinements added 2026-06-28, pending user spec review)
date: 2026-06-28
supersedes: v1.2-DESIGN.md "R3 — Hardware-aware engines & models" sketch (this is the committed R3 design); 15b-DESIGN.md (its 2-value STT_ENGINE selector + "faster-whisper first" are revised here to a 3-mode selector + Parakeet-int8-ONNX)
depends_on: R2 (VERIFIED 2026-06-28 — model lifecycle + ~6GB floor; v1.2-R2-VERIFY.md). R2's VRAM budget gates R3.
research_basis: 2-thread adversarial re-verification 2026-06-28 (Parakeet-tdt-0.6b-v2 facts + faster-whisper/Chatterbox) — see References. Corrects the v1.2-DESIGN VRAM/runtime/WER figures.
---

# v1.2 — R3 — Hardware-aware engines & models — Design

## Summary

R3 makes STT **hardware-aware** by turning the existing `STT_RUNTIME` device seam into a 3-mode
**engine** selector and adding a buffered Parakeet engine that eliminates the 15a end-of-speech
word-drop. It also lands the two thin adjacent seams the v1.2-DESIGN R3 sketch named (`TTS_ENGINE`
passthrough, hardware-derived `placement.py` VRAM), an **advise-only** hardware→profile mapping, and
an honestly-scoped `finalize()` tail-drop investigation. The whole 3-mode model lives **server-side**
behind the byte-unchanged WS contract; the agent plugin barely changes.

**Why R3 exists:** 15a UAT found streaming 0.6B STT systematically drops trailing words at
end-of-speech (the lookahead holds a tail the hard cutoff never completes); the surgical fixes
regressed. A **buffered** engine has no lookahead, so the cut-off is structurally absent. That — plus
silence-robustness — is the win, **not** a benchmark-WER leap (see D18).

## Engine model — `STT_ENGINE` is a 3-mode selector

`STT_ENGINE ∈ {streaming, buffered, hybrid}` is the **mode**; the existing `STT_RUNTIME ∈ {gpu, cpu}`
stays the **device** for the streaming Nemotron engine. Orthogonal axes.

| `STT_ENGINE` | Runs | Live partials | Authoritative final (→ LLM) | Endpointing | P50<1.0s hot path? |
|---|---|---|---|---|---|
| `streaming` | Nemotron only (today, byte-for-byte) | yes (Nemotron) | Nemotron `finalize` (~100ms flat) | Nemotron + VAD | **yes** |
| `buffered` | Parakeet-int8-ONNX only | none | Parakeet transcribe at EOU | **VAD (energy) only** | no (accuracy mode) |
| `hybrid` | Nemotron **+** Parakeet | yes (Nemotron, display-only) | **Parakeet** transcribe at EOU | Nemotron + VAD (**unchanged**) | no (accuracy mode, *feels* live) |

In `hybrid` the two engines split the work: **Nemotron owns liveness + endpointing authority**
(preserving the load-bearing endpoint-authority invariant), **Parakeet owns the accurate, cut-off-free
transcript the LLM consumes**. Partials are cosmetic; the final is authoritative.

## Per-tier defaults (resolved at install, advise-only)

| Tier | Hardware | Default | Opt-in | Parakeet device |
|---|---|---|---|---|
| **Floor** | ~6–8GB / CPU-only | `buffered` (finalize-only) | `streaming` (lower latency) | CPU-ONNX |
| **Capable** | ~12–16GB (Gemma-E2B class) | `hybrid` | `streaming` | GPU-ONNX **if `placement.py` fits, else CPU-ONNX** |
| **Best** | 16GB+ | `hybrid` | `streaming` | GPU-ONNX |

Floor gets buffered finalize-only (can't afford two engines on a contended CPU). The GPU tiers get
hybrid by default — accuracy-first wherever a GPU exists (D14) — with `streaming` as the one-flag
latency opt-in that restores the P50<1.0s contract (D15).

## Architecture — server-side composition over the existing seam

**Chosen: server-side composition.** Rejected: *agent-side* (two STT plugins merged in the agent —
reaches into the LiveKit turn pipeline + endpoint-authority invariant, riskier) and *single
mega-backend* (hides three behaviors in one module, kills the per-module seam's testability).

The server (`stt/server.py`) gains a **`primary` backend** (drives deltas + EOU) and an optional
**`final` backend** (authoritative transcript), both via the existing `importlib`-by-name dispatch.
Pure composition over today's 5-callable seam (`load_model / new_stream_state / decode_chunk /
finalize / reset_turn_state`):

| `STT_ENGINE` | primary (deltas + EOU) | final (authoritative) |
|---|---|---|
| `streaming` | `backend_nemo` / `backend_onnx` (Nemotron) | same — Nemotron `finalize` |
| `buffered` | `backend_parakeet` (accumulate PCM, **no deltas**, energy-EOU) | `backend_parakeet` (transcribe buffer) |
| `hybrid` | `backend_nemo` (Nemotron deltas + EOU, **unchanged**) | `backend_parakeet` (transcribe accumulated PCM at EOU) |

### Components
- **`stt/backend_parakeet.py` (new)** — implements the same 5 callables, but `decode_chunk` *appends
  PCM to `state["_turn_pcm"]`* and returns no growth; `finalize` runs **Parakeet-tdt-0.6b-v2 int8
  ONNX** over the whole buffer once (encoder ONNX + a TDT greedy-decode loop in numpy — the **same
  ORT + numpy-decode pattern `backend_onnx` already uses** for the Nemotron CPU port). A
  module-level **`STREAMS = False`** capability flag is the only new seam concept.
- **Server EOU policy keys on `STREAMS`** (today's logic, generalized): `STREAMS=True` → emit deltas
  + text-stall EOU (`_emit_delta`, unchanged). `STREAMS=False` → no deltas + a **cheap RMS
  energy-silence EOU** (numpy mean-square per chunk, reusing `ENDPOINT_SILENCE_MS`) — there is no
  growing transcript to stall-detect, so silence is detected acoustically. This is the *one* new
  piece of server logic. `hybrid` needs none (Nemotron still drives EOU).
- **`STT_ENGINE` gates which `load_model()` fires at lifespan startup** → `streaming` loads only
  Nemotron (zero Parakeet RAM/VRAM even though the image carries it). No wasted memory, disk-cost only.
- **Device for Parakeet** comes from `resolve_parakeet_device(env)` (see Hardware awareness) →
  selects the onnxruntime EP (`CPUExecutionProvider` vs `CUDAExecutionProvider`).

### Agent-plugin impact: minimal
The WS contract (`ready` / `delta` / `final`) is byte-identical. `buffered` simply omits `delta`s;
`hybrid` sources `final` from Parakeet. The plugin (`agent/nemo_stt.py`) already **commits a turn on
FINAL alone** (`_audio_transcript` is filled only by finals; interims are "still-speaking" signals),
so a zero-interim turn commits cleanly. Two small touches: a per-engine **metrics label** (so `stt_ms`
attributes correctly) and wiring the **autonomous-final latency span** server-side (the buffered/hybrid
EOU final has no flush sentinel, so today's `duration` would read 0.0 — the G3 gate needs the real
EOU→final span).

### Image / compose isolation — no new image
Because the buffered engine is **lean ONNX-int8 (no torch, no CTranslate2)**, both existing STT images
just gain the `backend_parakeet` module + its int8 ONNX weights, gated by `STT_ENGINE`:
- **`stt/Dockerfile` (GPU)** — already NeMo/torch (Nemotron). Add `onnxruntime-gpu` + Parakeet ONNX
  weights → serves `streaming` + `hybrid` (Parakeet on the CUDA EP). One process.
- **`stt/Dockerfile.cpu` (lean ORT)** — already onnxruntime CPU (Nemotron ONNX). Add Parakeet ONNX
  weights → serves Floor `streaming` (Nemotron) **and** `buffered` (Parakeet) from one image; the mode
  picks which `load_model` fires. No torch added.
- Compose **profiles** stay as today's `stt-gpu`-vs-default split; `STT_ENGINE` selects the mode within
  the chosen image. (Parakeet ONNX weights are ~0.6–0.7GB on disk, loaded only when the mode needs them.)

### Error boundaries
- Parakeet model/weights load failure → `SystemExit` at startup (mirrors the `STT_MODEL` KeyError
  posture) → `/health` stays 503 → the agent's `service_healthy` gate never starts on a broken image.
- Transcribe exception → emit `{"type":"error"}` **and** a VAD-driven empty `final` so the turn never
  deadlocks (the framework waits for a FINAL).
- A **max-buffer cap** (`STT_BUFFERED_MAX_S`, default 60s) bounds `state["_turn_pcm"]` so a missed EOU
  can't grow memory unbounded; on cap, force-finalize.
- Empty/garbled Parakeet output → empty FINAL → turn still commits (VAD-driven), LLM sees empty (same
  contract as today's empty-STT boundary).

## Hardware awareness

Three coordinated changes; `placement.py` stays pure (never probes hardware, never raises,
sandbox-runnable via `_self_check`) and `gpu-doctor.sh` stays advise-only (never mutates `.env`,
always exits 0).

### A. `placement.py` — env-derived total + a Parakeet fit gate
- `VRAM_TOTAL_MB` becomes an **env read** threaded through the existing `env` Mapping (default `16384`
  when unset) — *not* an in-module `nvidia-smi` probe (that would break purity). The real total is
  supplied by gpu-doctor. The `STT_HEADROOM_MEASURED` gate stays the safety net: even if the total
  defaults high on an 8GB card, the GPU branch stays CPU until an operator measures and flips the flag.
- New pure `resolve_parakeet_device(env) → "gpu"|"cpu"` for `hybrid` (Nemotron **and** Parakeet
  co-resident): `"gpu"` iff `max(LLM_PEAK) + KOKORO_MB + STT_GPU_MB + PARAKEET_GPU_MB ≤ ceiling`, else
  `"cpu"`. Same `STT_FORCE_CPU` / `STT_HEADROOM_MEASURED` gating posture as `resolve_stt_placement`.
- `PARAKEET_GPU_MB` ships as a **measured-placeholder ≈ 1200 MB** (int8 ONNX encoder ~622MB + decoder/
  joiner + CUDA context), gated like `KOKORO_MB` was; the G2 operator gate pins it.
- `_self_check()` extended with the new function + an env-supplied-total case. Stays sandbox-runnable.

**Worked example** (honest int8 numbers — why hybrid now fits and the Capable default stays acceptable):

| Card | Stack: worst-LLM + Kokoro + Nemotron + Parakeet(int8) | Ceiling (total−1GB) | → Parakeet device |
|---|---|---|---|
| 12GB Capable | 8912 + 954 + 2400 + 1200 = **13466** | 11264 | **CPU-ONNX** (frees GPU; ~30× realtime) |
| 16GB Best | 8912 + 954 + 2400 + 1200 = **13466** | 15360 | **GPU-ONNX** (CUDA EP; ~80× realtime) |

Both rows use the **worst-case LLM** `max(LLM_PEAK)=8912` (E4B/Better) — the same no-mid-session-thrash
lock `resolve_stt_placement` already uses, so a Fast↔Better swap can never strand placement; only the
card's ceiling differs. A 12GB Capable card therefore runs Parakeet CPU-ONNX (~30× realtime → ~0.67s
for a 20s turn — fine for accuracy mode); a 16GB Best card runs it on the CUDA EP (~0.1–0.3s). Neither
busts VRAM.

### B. `gpu-doctor.sh` — VRAM → profile advice (prints, never writes)
Extends today's nvidia-smi vendor+VRAM detection to print a recommended `.env` snippet, using **named
readonly thresholds** (no magic numbers, matching its current style):

| Detected | Tier | Recommended `.env` snippet |
|---|---|---|
| no GPU / CPU-only | Floor | `STT_ENGINE=buffered` |
| VRAM < 16GB (8/12GB) | Floor→Capable | `STT_ENGINE=` buffered on ≤8GB, hybrid on 12GB; `VRAM_TOTAL_MB=<detected>` |
| VRAM ≥ 16GB | Best | `STT_ENGINE=hybrid`; `VRAM_TOTAL_MB=<detected>` |

It still **never mutates `.env`, always exits 0** (D16). `resolve_parakeet_device` is the shared
decision logic gpu-doctor uses to *recommend* and the agent's `build_session` uses to *pick the
buffered Parakeet EP* (mirroring how `resolve_stt_placement` picks the STT URL today). Unknown/garbled
VRAM → gpu-doctor recommends the conservative Floor snippet; `placement.py` keeps returning CPU. No
detection bug can route a too-big stack onto a small card.

## Adjacent seams

### `TTS_ENGINE` seam — designed, not built (YAGNI)
Add `TTS_ENGINE` env (default `kokoro`) + a compose profile mirroring `STT_ENGINE`; the agent
generalizes today's `KOKORO_BASE_URL` (`agent/main.py:75`) into a per-engine TTS base-URL lookup.
**Only Kokoro ships.** The v1.3 target is **Chatterbox-Turbo (350M, MIT)** — its streaming path, not the
base 0.5B (offline-only). Avatar caveat (documented decision): non-Kokoro engines have no
`/dev/captioned_speech`, so word-accurate Path-B lip-sync degrades to **Path-A** (the existing
per-utterance fallback); the **voice path is unaffected** (standard OpenAI `/v1/audio/speech`), and the
Phase-14 voice-only invariant holds. Scope guard: **one env + a documented profile**, no `TTSEngine`
base class (no second concrete use today).

### `finalize()` tail-drop investigation — honest scope (not a promised fix)
The canonical NeMo fix regressed in 15a; the two candidates (`STT_THREAD_PRED_OUT`,
`STT_FINALIZE_PAD` in `backend_common.py`) stay **dormant, default-OFF**. R3's deliverable is an
**instrumented GPU runbook + an operator gate (G5)**, not a one-liner. The escape hatch makes this
low-risk: `buffered`/`hybrid` have no lookahead → no held tail → the cut-off is structurally absent in
the new defaults. So R3 either (a) confirms a candidate fixes streaming on GPU under a
**no-WER-regression** gate and promotes it, or (b) documents streaming's tail-drop as *mitigated by the
buffered/hybrid default*. The keep-or-strip-the-dormant-code call is deferred to G5's outcome.

## Docs + headline re-characterization (D15)
R3 updates `CLAUDE.md` / `PROJECT.md` / `README.md` / `STACK.md`: **P50<1.0s is the streaming-mode
guarantee**; hybrid/buffered are accuracy-mode with a measured EOU cost. Adds the **"pick your STT by
hardware"** table (3 modes × 3 tiers) and the honest value framing (D18: cut-off elimination +
robustness, not WER). This reframing is a deliverable, not an afterthought — it is the honest
counterpart to choosing hybrid-default on the GPU tiers.

## Decisions

| # | Decision | Rationale |
|---|---|---|
| D13 | `STT_ENGINE = {streaming, buffered, hybrid}`; `hybrid` = Nemotron partials + endpointing + **Parakeet authoritative final** | Revises 15b's 2-value selector. Splits liveness (Nemotron) from accuracy (Parakeet) so hybrid keeps live partials *and* a cut-off-free final |
| D14 | Per-tier defaults: Floor=`buffered`, **Capable/Best=`hybrid`**; `streaming` = the GPU-tier opt-in; `streaming` = the Floor opt-in | Accuracy-first wherever a GPU exists (user). D12's Floor=buffered stands; revises D5/D6's "streaming default everywhere" |
| D15 | P50<1.0s re-characterized as the **streaming-mode** guarantee; hybrid/buffered = accuracy-mode at a measured EOU cost; docs updated | Honest counterpart to hybrid-default; the metric is preserved (as the opt-in), not silently broken |
| D16 | Install resolution is **advise-only** — gpu-doctor recommends, R7's installer writes `.env` | Preserves gpu-doctor's never-mutate/always-exit-0 safety invariant; no R3/R7 overlap |
| D17 | Buffered engine = **Parakeet-tdt-0.6b-v2 int8 ONNX** (ORT + numpy TDT decode, sherpa-onnx-style), CPU or CUDA EP; **NeMo-fp16 Parakeet deferred to ≥24GB** (not built) | Research: torch-CPU Parakeet ~1.4× realtime (unusable); int8 ONNX ~30× CPU / ~80× GPU, ~1GB, fits 16GB hybrid; reuses the existing lean ORT image/pattern (no torch, no new image). **Revised by D22 (gate-enablement): the buffered body uses the canonical sherpa-onnx OfflineRecognizer, not a hand-rolled ORT+numpy TDT decode. See v1.2-R3-GATES-DESIGN.md.** |
| D18 | buffered/hybrid chosen for **cut-off elimination + silence-robustness**, NOT WER | Independent leaderboard: Parakeet-v2 6.91% (int8 ~7.8%) ≈ Nemotron streaming 6.93–7.07%. The durable win is no-lookahead (no held tail) + trained-to-emit-empty-on-silence |
| D19 | Pin **`parakeet-tdt-0.6b-v2`** (NOT v3) + **`nemo-toolkit` 2.7.3** (never `:latest`); license **CC-BY-4.0** (attribution) | v3 is multilingual and *regresses* English (6.32% + reported sentence-drops). Pin discipline matches `STACK.md` |
| D20 | `TTS_ENGINE` seam targets **Chatterbox-Turbo** (350M, MIT) for v1.3; seam built, engine not | Base 0.5B Chatterbox is offline-only; Turbo is the streaming variant. Make v1.3 a config+image change |

## Model & version pins (verified 2026-06-28)
- **Buffered STT:** `nvidia/parakeet-tdt-0.6b-v2` — 600M FastConformer-TDT, **English-only**, offline;
  CC-BY-4.0 (attribution); native punctuation/capitalization + word timestamps. WER 6.05% self-reported
  / **6.91% independent** (8-dataset Open-ASR avg); int8 ≈ +13% rel. **Do NOT use v3** (multilingual,
  English regression). int8 ONNX via the sherpa-onnx export (encoder 622MB / decoder 6.9MB / joiner
  1.7MB) or an equivalent ORT graph.
- **Streaming STT:** unchanged — the Nemotron streaming sidecar (`nemo-stt` / `nemo-stt-cpu`).
- **Runtime:** `nemo-toolkit` 2.7.3-class on the GPU image; `onnxruntime` / `onnxruntime-gpu` for the
  Parakeet ONNX path. Pin exact tags in `STACK.md`; never `:latest`.
- **TTS (v1.3, not built):** Resemble **Chatterbox-Turbo** 350M, MIT, streaming (~75ms dedicated-GPU /
  ~472ms first-chunk on a 4090 — will not hold under shared-GPU contention).

## Carried risks & measurement gates
1. **Buffered finalize latency on long interview turns is unmeasured** (D12's "measure before locking").
   GPU ~0.1–0.3s expected; CPU-ONNX ~0.67s for 20s — both plausible, neither verified. → **G1**.
2. **Hybrid co-residency VRAM** on 12 vs 16GB pins `PARAKEET_GPU_MB` and validates `resolve_parakeet_device`. → **G2**.
3. **int8 WER cost** (~+13% rel) vs the cut-off win is a real-audio judgement, not a benchmark. The
   Floor's *current* streaming-CPU already sits ~7–8% **and** drops words; buffered trades a hair of WER
   for zero word-drop. Confirm on real UAT audio. → **G1**.
4. **Floor CPU contention** — Parakeet-ONNX + LLM (+ optional TTS-CPU) on shared cores; measure P50/feel. → **G1**.
5. **Energy-EOU robustness** — the RMS silence trigger for `buffered` must not fire mid-utterance on a
   pause; tune against `ENDPOINT_SILENCE_MS`. Sandbox-unit-testable on synthetic PCM.

## Verification plan — code-complete + operator gates
This sandbox has no GPU/Docker/NeMo (mirrors Phases 9/10/R2), so R3 ships **code-complete** with a new
`v1.2-R3-VERIFY.md` operator-gate doc.
- **Sandbox-runnable now:** `placement.py` extended `_self_check` (env-total + `resolve_parakeet_device`);
  `server.py` byte-compiles GPU-less; a `test_dispatch.py`-style stubbed test for `STT_ENGINE` mode
  selection + the `STREAMS=False` no-delta path + the RMS energy-EOU trigger; a Parakeet offline-transcribe
  unit on stubbed ONNX sessions.
- **Operator gates (`v1.2-R3-VERIFY.md`):**
  - **G1** — buffered Parakeet WER **+ finalize latency on long interview turns** (Floor CPU-ONNX +
    Best GPU-ONNX); the D12 "measure before locking" gate + real-audio cut-off/robustness check.
  - **G2** — hybrid co-residency VRAM on 12GB vs 16GB; pins `PARAKEET_GPU_MB`, validates the fit table.
  - **G3** — hybrid EOU→final latency (the new authoritative-final span; wire the server-side timing).
  - **G4** — `streaming` mode **byte-identical** to today (no regression on the default-latency path).
  - **G5** — the `finalize()` candidate investigation (promote-with-no-WER-regress, or document mitigated).

## Out of scope → v1.3+
- **NeMo-fp16 Parakeet** (best WER, ~5GB) as a built ≥24GB high-accuracy engine — documented, not built.
- **Chatterbox-Turbo TTS** — seam designed in R3, engine built in v1.3.
- **Multilingual STT** (Parakeet-v3 / Whisper) — English-only in v1.2.
- **faster-whisper engine** — documented alternative only (dropping it removes CTranslate2 entirely;
  Whisper still wins on multilingual/>24min/CPU-portability if those ever become requirements).
- **Live in-app engine switching** — engine choice is install-time (YAGNI).

## Per-stream success criteria
- `STT_ENGINE` selects `streaming` / `buffered` / `hybrid`; per-tier defaults resolve at install
  (advise-only); `streaming` is byte-identical to today (G4).
- Floor `buffered` and GPU-tier `hybrid` produce **cut-off-free** finals on real UAT audio (G1), within
  the accuracy-mode latency contract.
- `placement.py` derives the VRAM total from env and resolves Parakeet GPU/CPU via the measured fit gate
  (G2); the `STT_HEADROOM_MEASURED` safety lock holds.
- The `TTS_ENGINE` seam exists (Kokoro-only shipped); the `finalize()` tail-drop is fixed-with-no-regress
  or documented-as-mitigated (G5); docs reframe P50<1.0s honestly (D15).

## References (verified 2026-06-28)
- Parakeet model card — https://huggingface.co/nvidia/parakeet-tdt-0.6b-v2 (params, WER, license, PnC,
  24-min single pass)
- NeMo ASR models — https://docs.nvidia.com/nemo-framework/user-guide/latest/nemotoolkit/asr/models.html
- Streaming-degradation study (BSF, "nearly doubles") — https://arxiv.org/html/2604.14493v2
- L4 batch-1 VRAM/RTF benchmark — https://www.e2enetworks.com/blog/benchmarking-asr-models-nvidia-l4-parakeet-whisper-nemotron
- sherpa-onnx pre-built int8 Parakeet ONNX + TDT decode — https://k2-fsa.github.io/sherpa/onnx/pretrained_models/offline-transducer/nemo-transducer-models.html
- Independent Open-ASR-Leaderboard repro (6.91%) — https://arxiv.org/html/2510.06961v4
- v3 English regression — https://huggingface.co/nvidia/parakeet-tdt-0.6b-v3 ; nemo-toolkit 2.7.3 — https://pypi.org/project/nemo-toolkit
- CTranslate2 cuDNN-8/9 vs PyTorch conflict — https://github.com/OpenNMT/CTranslate2/issues/1780
- Chatterbox (MIT, Turbo 350M, streaming) — https://github.com/resemble-ai/chatterbox

## Code touchpoints (for the implementation plan)

> **B/C refined 2026-06-28** — the authoritative Plan B/C scope is the **"Plan B/C build
> refinements"** section at the END of this doc. It DROPS `resolve_parakeet_device` +
> `PARAKEET_GPU_MB` + the `TTS_ENGINE` code; the `agent/placement.py` / `agent/main.py` /
> `TTS_ENGINE` lines below are the original A+B+C sketch, kept for history.
- `stt/server.py` — primary/final backend composition; `STREAMS`-keyed EOU policy; energy-EOU; max-buffer cap.
- `stt/backend_parakeet.py` (new) — int8-ONNX Parakeet buffered backend (5 callables, `STREAMS=False`).
- `stt/backend_common.py` — shared constants (energy-EOU threshold, max-buffer); dormant finalize candidates stay.
- `stt/Dockerfile`, `stt/Dockerfile.cpu` — add Parakeet ONNX weights + onnxruntime(-gpu); no new image.
- `agent/placement.py` — env `VRAM_TOTAL_MB`; `resolve_parakeet_device`; extended `_self_check`.
- `agent/main.py` — `STT_ENGINE` wiring; pick buffered EP via `resolve_parakeet_device`; `TTS_ENGINE`/base-URL lookup.
- `agent/nemo_stt.py` — per-engine metrics label; autonomous-final latency span.
- `scripts/gpu-doctor.sh` — VRAM→profile advice snippet (named thresholds; never writes).
- `docker-compose.yml`, `.env.example` — `STT_ENGINE`, `VRAM_TOTAL_MB`, `TTS_ENGINE`, profiles.
- Docs — `CLAUDE.md`, `PROJECT.md`, `README.md`, `.planning/research/STACK.md` (pins + headline reframe + STT-by-hardware table).

## Plan B/C build refinements (2026-06-28, brainstorm)

Plan A (STT engine seam) is merged. Brainstorming B + C surfaced two simplifications to this design;
both reduce code, neither changes the engine model (D13–D20 stand). **B and C ship as ONE combined
implementation plan** — both are small, they share the docs + VERIFY deliverable, and C's "pick your
STT by hardware" table is the documentation of B's advice logic.

### Delta 1 (Plan B) — hardware awareness is ADVISE-ONLY-IN-BASH (drop `resolve_parakeet_device`)
The buffered-Parakeet device is read **server-side** from `STT_BUFFERED_DEVICE` at container boot
(`backend_parakeet.py`); the agent only chooses which STT *URL* it connects to and cannot change a
running server's onnxruntime EP, and `gpu-doctor.sh` is bash. So the design's "shared
`resolve_parakeet_device(env)` in `placement.py` that `build_session` uses to pick the EP" has **no
runtime consumer** and would duplicate fit-math across Python and bash. Resolution:
- **Dropped:** `resolve_parakeet_device`, the `PARAKEET_GPU_MB` constant, and the agent
  `build_session` change (all from the "Hardware awareness §A" sketch).
- **Kept (smaller):** `placement.py` makes `VRAM_TOTAL_MB` **env-derived** (default 16384) so
  `resolve_stt_placement`'s ceiling is correct on a measured 8/12GB card — consumed by the existing
  GPU-fit gate, so it earns its place. `gpu-doctor.sh` owns the Parakeet-device + engine
  recommendation as **named readonly bash thresholds** (its existing advise-only / always-exit-0 posture).
- **Gate reframe:** **G2** becomes "measure Parakeet GPU footprint to TUNE the gpu-doctor VRAM
  threshold," not "pin the `PARAKEET_GPU_MB` Python constant."

### Delta 2 (Plan C) — DEFER the `TTS_ENGINE` code seam to v1.3 (docs-only now)
The only consumer of a second TTS engine is v1.3 Chatterbox-Turbo (not built; the base 0.5B is
offline-only — the wrong variant). A seam with one implementation violates "no abstraction without
≥2 concrete uses today," and adding the ~10 lines now makes v1.3 no easier. Resolution:
- **Dropped from R3:** the `TTS_ENGINE` env, the per-engine base-URL lookup generalizing
  `KOKORO_BASE_URL` (`agent/main.py:75`), and the TTS compose profile.
- **Kept:** one paragraph in the docs reframe documenting the `TTS_ENGINE`-mirrors-`STT_ENGINE`
  seam as the **v1.3 path** (design intent preserved as documentation, not speculative code).
- D20 stands as the v1.3 target; only its *build-in-R3* timing changes.

### Final combined-plan scope (supersedes §A and the "Adjacent seams: TTS_ENGINE" build items above)

**Plan B — hardware awareness (advise-only)**
- `agent/placement.py` — `VRAM_TOTAL_MB` env-derived via the `env` Mapping (default 16384); thread it
  through the ceiling / `_gpu_fits`; extend `_self_check` with a sub-16GB case. Stays pure (no
  `nvidia-smi`), never raises.
- `scripts/gpu-doctor.sh` — extend the advice block to print recommended `STT_ENGINE` +
  `STT_BUFFERED_DEVICE` + `VRAM_TOTAL_MB` per detected VRAM tier; named readonly thresholds (reuse
  `VRAM_FLOOR_MB`=16384, add `VRAM_HYBRID_MB`=12288); never writes `.env`, always exits 0. Tiers:
  no GPU → `buffered`; <12GB → `buffered`/cpu; 12–<16GB → `hybrid`/cpu/`VRAM_TOTAL_MB`;
  ≥16GB → `hybrid`/gpu/`VRAM_TOTAL_MB`.
- `.env.example` — add the `VRAM_TOTAL_MB` knob/note (the `STT_ENGINE`/`STT_BUFFERED_DEVICE` knobs
  already landed in Plan A).
- Tests — `placement.py _self_check` sub-16GB case; a small assert-based check of gpu-doctor's
  tier→snippet mapping (stubbed VRAM in → assert the recommended lines out).

**Plan C — docs reframe + finalize runbook + VERIFY doc**
- Docs (`CLAUDE.md`, `PROJECT.md`, `README.md`, `.planning/research/STACK.md`) — P50<1.0s =
  **streaming-mode** guarantee; "pick your STT by hardware" table (3 modes × 3 tiers); D18 honest
  framing (cut-off elimination + silence-robustness, **not** WER); Parakeet pins (v2 not v3,
  CC-BY-4.0 attribution, nemo-toolkit 2.7.3); one paragraph on the **deferred** `TTS_ENGINE` seam.
- `finalize()` tail-drop — the dormant `STT_THREAD_PRED_OUT` / `STT_FINALIZE_PAD` candidates stay
  default-OFF in `backend_common.py` (confirmed present); deliverable is the instrumented GPU runbook
  + **G5** (no promised fix); no sandbox code change.
- `v1.2-R3-VERIFY.md` (new) — operator-gate doc consolidating **G1** (buffered WER + finalize
  latency), **G2** (hybrid co-residency VRAM → tune the gpu-doctor threshold), **G3** (hybrid
  EOU→final latency), **G4** (streaming byte-identical), **G5** (finalize investigation). Signed on
  the GPU host.

**Out of scope (unchanged):** `TTS_ENGINE` code, `resolve_parakeet_device`, NeMo-fp16 Parakeet,
multilingual STT, live in-app engine switching.
